version: '3.11'

services:
  app:
    build:
      context: .
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8080"
    ports:
      - 8080:8080
    volumes:
      - ./app:/app
      - ./data/web:/vol/web
    environment:
      - SECRET_KEY=devsecretkey
      - DEBUG=1

